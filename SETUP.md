# 🚀 Инструкция по развертыванию сайта 3DPC

Подробное руководство по локальной установке и настройке проекта.

## 📋 Требования

- **Node.js** версии 18 или выше
- **npm** или **yarn** (менеджер пакетов)
- **Git** для клонирования репозитория
- **PostgreSQL** (опционально, для работы с базой данных)

## 🔧 Установка и запуск

### Шаг 1: Клонирование репозитория

```bash
git clone [URL_вашего_репозитория]
cd [название_папки]
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

Эта команда установит все необходимые библиотеки из `package.json`.

### Шаг 3: Запуск проекта

```bash
npm run dev
```

Сайт откроется автоматически на `http://localhost:5173`

### Шаг 4: Сборка для продакшена

```bash
npm run build
```

Готовые файлы появятся в папке `dist/` - их можно загрузить на любой хостинг.

## 🗄️ Настройка базы данных (опционально)

По умолчанию сайт работает с демо-данными и **не требует базу данных**.

Если вы хотите использовать реальную PostgreSQL базу:

### Установка PostgreSQL

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
```

**macOS:**
```bash
brew install postgresql
brew services start postgresql
```

**Windows:**
Скачайте установщик с [postgresql.org](https://www.postgresql.org/download/windows/)

### Создание базы данных

```bash
# Войдите в PostgreSQL
sudo -u postgres psql

# Создайте базу данных
CREATE DATABASE 3dpc_db;

# Создайте пользователя (опционально)
CREATE USER 3dpc_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE 3dpc_db TO 3dpc_user;
```

### Применение миграций

Структура таблиц находится в папке `db_migrations/`. Выполните SQL-файлы по порядку:

```bash
# Пример для версии 1
psql -d 3dpc_db -f db_migrations/V1__initial_schema.sql

# Пример для версии 2
psql -d 3dpc_db -f db_migrations/V2__add_clients.sql
```

Или вручную через psql:
```bash
psql 3dpc_db
\i db_migrations/V1__initial_schema.sql
\i db_migrations/V2__add_clients.sql
```

### Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
DATABASE_URL=postgresql://username:password@localhost:5432/3dpc_db
```

Замените `username`, `password` на ваши данные.

## 📁 Структура проекта

```
3dprint-customs-portal/
├── src/                      # Исходный код
│   ├── components/           # React компоненты
│   │   ├── ui/              # UI компоненты (кнопки, карточки)
│   │   ├── Hero.tsx         # Главный экран
│   │   ├── Services.tsx     # Секция услуг
│   │   └── ...
│   ├── pages/               # Страницы
│   │   ├── Home.tsx         # Главная страница
│   │   └── AdminPanel.tsx   # Админ-панель
│   ├── App.tsx              # Главный компонент с роутингом
│   ├── main.tsx             # Точка входа
│   └── index.css            # Глобальные стили
├── backend/                  # Облачные функции (опционально)
│   ├── orders/              # Функция обработки заказов
│   ├── portfolio/           # Функция портфолио
│   └── func2url.json        # Маппинг функций к URL
├── db_migrations/           # SQL миграции базы данных
├── public/                  # Статические файлы
├── package.json             # Зависимости проекта
├── vite.config.ts           # Конфигурация Vite
├── tailwind.config.ts       # Конфигурация Tailwind CSS
└── tsconfig.json            # Конфигурация TypeScript
```

## 🎨 Технологии

### Frontend
- **React 18** - библиотека для UI
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик проекта
- **Tailwind CSS** - utility-first CSS фреймворк
- **shadcn/ui** - готовые UI компоненты
- **Lucide React** - иконки
- **React Router** - маршрутизация

### Backend (на серверах poehali.dev)
- **Cloud Functions** - serverless функции
- **Python 3.11** / **TypeScript** - языки для функций
- **PostgreSQL** - реляционная база данных

## 🔐 Админ-панель

Админ-панель доступна по адресу: `http://localhost:5173/#/admin`

### Демо-режим (по умолчанию)
- Введите любой пароль для входа
- Данные отображаются из локальных моков
- Изменения не сохраняются

### Для продакшена
Необходимо настроить:
1. Реальную систему аутентификации
2. Подключение к базе данных
3. Развертывание облачных функций на poehali.dev

## 📦 Полезные команды

```bash
# Разработка
npm run dev              # Запустить dev-сервер с hot-reload
npm run build            # Собрать проект для продакшена
npm run preview          # Просмотреть production-сборку локально
npm run lint             # Проверить код на ошибки

# Работа с Git
git status               # Проверить статус изменений
git add .                # Добавить все изменения
git commit -m "message"  # Создать коммит
git push                 # Отправить на GitHub

# Очистка (если возникли проблемы)
rm -rf node_modules package-lock.json
npm install              # Переустановить зависимости
```

## 🌐 Варианты деплоя

### 1. poehali.dev (рекомендуется)
Проект уже настроен:
- Автоматический деплой из GitHub
- Встроенная PostgreSQL база
- SSL сертификаты
- Облачные функции
- Не требует дополнительной настройки

### 2. Netlify
1. Зарегистрируйтесь на [netlify.com](https://www.netlify.com)
2. Подключите GitHub репозиторий
3. Настройки:
   - Build command: `npm run build`
   - Publish directory: `dist`
4. Deploy

### 3. Vercel
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Импортируйте GitHub репозиторий
3. Vercel автоматически определит Vite проект
4. Deploy

### 4. GitHub Pages
```bash
npm run build
# Загрузите содержимое dist/ в ветку gh-pages
```

### 5. VPS / Собственный сервер

**Nginx конфигурация:**
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    root /var/www/3dpc/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

**Деплой:**
```bash
# На сервере
cd /var/www/
git clone [your-repo]
cd 3dpc
npm install
npm run build
sudo systemctl restart nginx
```

## 🛠️ Разработка

### Добавление новой страницы

1. Создайте файл в `src/pages/NewPage.tsx`:
```tsx
export default function NewPage() {
  return (
    <div>
      <h1>Новая страница</h1>
    </div>
  );
}
```

2. Добавьте маршрут в `src/App.tsx`:
```tsx
import NewPage from './pages/NewPage';

// В компоненте App:
<Route path="/new" element={<NewPage />} />
```

### Изменение цветов

Цвета определены в `src/index.css` через CSS переменные:

```css
:root {
  --primary: 220 70% 50%;
  --secondary: 280 60% 50%;
  /* и т.д. */
}
```

Измените значения HSL для глобального изменения цветовой схемы.

### Добавление облачной функции

1. Создайте папку в `backend/my-function/`
2. Создайте `index.py` или `index.ts`:

```python
def handler(event, context):
    return {
        'statusCode': 200,
        'headers': {'Content-Type': 'application/json'},
        'body': '{"message": "Hello"}'
    }
```

3. Создайте `tests.json` для тестирования
4. Синхронизируйте с poehali.dev

## 🐛 Решение проблем

### Ошибка при npm install
```bash
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### Порт 5173 занят
```bash
npm run dev -- --port 3000
```

### Ошибки TypeScript
```bash
npm run lint
# Исправьте ошибки в коде
```

### Не загружаются стили
```bash
# Убедитесь, что Tailwind CSS настроен
# Проверьте tailwind.config.ts и postcss.config.js
```

### База данных не подключается
1. Проверьте, что PostgreSQL запущен: `sudo systemctl status postgresql`
2. Проверьте строку подключения в `.env.local`
3. Убедитесь, что миграции применены

## 📚 Дополнительные ресурсы

- [Документация React](https://react.dev)
- [Документация Vite](https://vitejs.dev)
- [Tailwind CSS](https://tailwindcss.com)
- [Документация poehali.dev](https://docs.poehali.dev)
- [Сообщество Telegram](https://t.me/+QgiLIa1gFRY4Y2Iy)

## 💡 Советы

1. **Git коммиты:** Делайте частые коммиты с понятными сообщениями
2. **Тестирование:** Проверяйте сайт в разных браузерах
3. **Производительность:** Используйте `npm run build` для оптимизации
4. **Безопасность:** Никогда не коммитьте `.env` файлы с секретами
5. **Резервные копии:** Регулярно делайте push в GitHub

## 📞 Поддержка

Если возникли вопросы:
- Проверьте [документацию poehali.dev](https://docs.poehali.dev)
- Спросите в [сообществе Telegram](https://t.me/+QgiLIa1gFRY4Y2Iy)
- Создайте Issue в GitHub репозитории

---

**Успешного развертывания! 🚀**

Создано с помощью [poehali.dev](https://poehali.dev)
